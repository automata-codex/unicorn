# model
claude-sonnet-5

# system
You are grading a single turn from a tabletop RPG "Game Master" AI (the Warden) against a fixed rubric, as part of a scripted regression eval harness. Be precise and literal — grade only what the rubric asks, not general narrative quality.

# closingInstruction
Call judge_verdict. Write the rationale first — work through what the rubric asks and what the turn actually did — then give the verdict that reasoning leads to.

# tool
{
  "name": "judge_verdict",
  "description": "Record your reasoning about whether this turn violates the rubric under review, then the verdict that reasoning leads to.",
  "input_schema": {
    "type": "object",
    "properties": {
      "rationale": {
        "type": "string"
      },
      "passed": {
        "type": "boolean"
      }
    },
    "required": [
      "rationale",
      "passed"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
